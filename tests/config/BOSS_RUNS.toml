[general]
name = "runs"                   # experiment name
wait = 60                       # waiting time between updates in live version
ref = "data/BOSS_test_data/zymo.fa"           # reference file
toml_readfish = "tests/config/BOSS_RUNS_RF.toml"

[live]
device = "MS00000"                     # position on sequencer
host = "localhost"              # host of sequencing device
port = 9502                     # port of sequencing device
data_wait = 100                 # wait for X Mb data before first update
prom = false

